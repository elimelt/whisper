 in this case, O2 actually optimize better than O3, because the optimizations are to some extent heuristic. OK? And there are also other kinds of optimization. You can have it do a profile guided optimization where you look at what the performance was and feed that back into the code, and then the.