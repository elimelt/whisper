 is vector instructions conservatively by default. So what you would if for particular if you're compiling for a particular machine, you can say use that particular machine. And here's some of the vectorization flags. You can say use the AVX instructions if you have.