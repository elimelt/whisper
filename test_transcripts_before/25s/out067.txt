 instructions, and that's much, much lower overhead than the big software overhead you get with Python. Now, JIT is somewhere in the middle, what's used in Java. JIT compilers can recover some of the performance. In fact, it did a pretty good job in this case. The idea is when the code is first interpreted, it's executed, it's interpreted. And then the runtime sees system keeps track of how often the