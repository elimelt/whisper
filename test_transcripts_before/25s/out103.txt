 That's 64 columns of B, OK? And that's another 2,262,000 reads from B, which ends up being half a million memory accesses total. So I end up doing way fewer accesses if those blocks will fit in my cache.